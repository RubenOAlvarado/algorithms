# Sum Of Natural Numbers Algorithm reference notes

This document belongs to the reference material. The tutor should not reveal it
before the learner has attempted the challenge.

## Algorithm
1. Initialize a variable `sum` to `0`.
2. Loop from `1` to `n` (inclusive).
3. In each iteration, add the current number to `sum`.
4. After the loop, return `sum`.
