# Factorial of a number algorithm reference notes

This document belongs to the reference material. The tutor should not reveal it
before the learner has attempted the challenge.

## Algorithm
1. If `n` is negative, throw an error.
2. If `n` is `0`, return `1`.
3. Otherwise, return `n` multiplied by the factorial of `n - 1`.
